{
    "scripts": {
        "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
        "start:backend": "cd backend   && npm install && node index.js",
        "start:frontend": "cd frontend && cd react-app && npm install && npm run dev"
    }
}